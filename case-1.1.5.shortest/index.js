const helper = require("../helper")
helper.reset()

// search the shortest string
function runnable(data) {
  return ""
}

helper.setCaseWithData("search the shortest string",
  helper.loadArrayString(),
  `Nam dui.`,
  runnable)